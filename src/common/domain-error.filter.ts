import{ArgumentsHost,Catch,ExceptionFilter,HttpException,HttpStatus,Logger}from'@nestjs/common';import type{Request,Response}from'express';import{DomainError}from'./domain-error';
@Catch()export class DomainErrorFilter implements ExceptionFilter{private readonly logger=new Logger(DomainErrorFilter.name);catch(error:unknown,host:ArgumentsHost){const http=host.switchToHttp(),request=http.getRequest<Request>(),response=http.getResponse<Response>();let status=HttpStatus.INTERNAL_SERVER_ERROR,code='INTERNAL_ERROR',title='An unexpected error occurred',detail:unknown;if(error instanceof DomainError){status=error.status;code=error.code;title=error.message}else if(error instanceof HttpException){status=error.getStatus();const body=error.getResponse();code=status===401?'UNAUTHORIZED':status===403?'FORBIDDEN':status===429?'RATE_LIMITED':'REQUEST_ERROR';title=typeof body==='string'?body:(body as{message?:string|string[]}).message instanceof Array?'Request validation failed':String((body as{message?:string}).message??error.message);detail=typeof body==='object'?(body as{message?:unknown}).message:undefined}else this.logger.error(error instanceof Error?error.stack:String(error));response.status(status).type('application/problem+json').json({type:`https://api.goverifeye.com/problems/${code.toLowerCase()}`,title,status,code,instance:request.originalUrl,correlationId:response.getHeader('x-correlation-id'),...(detail?{detail}:{})})}}
export class NotFoundDomainError extends DomainError{constructor(resource:string){super(`${resource} was not found`,'NOT_FOUND',HttpStatus.NOT_FOUND)}}
