import{Injectable,ServiceUnavailableException}from'@nestjs/common';import{createClient,SupabaseClient}from'@supabase/supabase-js';import{DomainError}from'../common/domain-error';
@Injectable()export class DocumentStorageService{private client?:SupabaseClient;private get storage(){const url=process.env.SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key||key.startsWith('replace-'))throw new ServiceUnavailableException('Supabase Storage is not configured');return(this.client??=createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}})).storage.from(process.env.SUPABASE_STORAGE_BUCKET??'compliance-documents')}async signedUpload(path:string){const{data,error}=await this.storage.createSignedUploadUrl(path,{upsert:false});if(error)throw new ServiceUnavailableException('Could not create a signed upload URL');return data}async signedDownload(path:string){const{data,error}=await this.storage.createSignedUrl(path,Number(process.env.STORAGE_DOWNLOAD_TTL_SECONDS??300));if(error)throw new ServiceUnavailableException('Could not create a signed download URL');return data}async download(path:string){const{data,error}=await this.storage.download(path);if(error)throw new DomainError('Uploaded document was not found','DOCUMENT_OBJECT_NOT_FOUND',404);return Buffer.from(await data.arrayBuffer())}async remove(path:string){const{error}=await this.storage.remove([path]);if(error)throw new ServiceUnavailableException('Could not remove the stored document')}}
