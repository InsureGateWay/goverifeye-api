import { Module } from '@nestjs/common'; import { TypeOrmModule } from '@nestjs/typeorm'; import { CodesController } from './codes.controller'; import { CodeBatchEntity, VerificationCodeEntity, VerificationEventEntity } from './code.entity'; import { CodesService } from './codes.service'; import { CryptographicCodeGenerator } from './cryptographic-code-generator.service';
@Module({ imports: [TypeOrmModule.forFeature([CodeBatchEntity, VerificationCodeEntity,VerificationEventEntity])], controllers: [CodesController], providers: [CodesService, CryptographicCodeGenerator], exports: [CodesService] }) export class CodesModule {}
