import { Column, Entity, Index } from 'typeorm'; import { BaseEntity } from '../database/base.entity';
@Entity('users') export class UserEntity extends BaseEntity { @Column({ unique: true }) email!: string; @Column() passwordHash!: string; @Column({ default: '' }) firstName!: string; @Column({ default: '' }) lastName!: string; @Column({ nullable: true }) phone?: string; @Column('uuid') @Index() organizationId!: string; @Column({ default: 'admin' }) role!: string; @Column({ default: true }) isActive!: boolean; }
@Entity('otp_challenges') export class OtpEntity extends BaseEntity { @Column() @Index() email!: string; @Column() hash!: string; @Column({ type: 'timestamp' }) expiresAt!: Date; @Column({ default: 0 }) attempts!: number; @Column({ default: false }) consumed!: boolean; }
@Entity('auth_sessions') export class AuthSessionEntity extends BaseEntity { @Column('uuid') @Index() userId!:string; @Column('uuid') @Index() organizationId!:string; @Column() refreshTokenHash!:string; @Column({type:'timestamp'}) expiresAt!:Date; @Column({type:'timestamp',nullable:true}) revokedAt?:Date; @Column({default:0}) rotation!:number; }
@Entity('password_reset_challenges') export class PasswordResetChallengeEntity extends BaseEntity { @Column('uuid') @Index() userId!:string; @Column() @Index() email!:string; @Column() codeHash!:string; @Column({type:'timestamp'}) expiresAt!:Date; @Column({default:0}) attempts!:number; @Column({default:false}) consumed!:boolean; }
